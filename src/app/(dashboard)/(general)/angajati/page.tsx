import Table from "@/components/ui/Table";
import { Box } from "@mui/material";

const Employees = () => {
  return (
    <Box>
      <Table columns={[]} data={[]} />
    </Box>
  );
};

export default Employees;
