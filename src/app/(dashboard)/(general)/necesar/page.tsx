"use client";
import Table from "@/components/ui/Table";
import { Box } from "@mui/material";

const Requirement = () => {
  return (
    <Box>
      <Table columns={[]} data={[]} />
    </Box>
  );
};

export default Requirement;
