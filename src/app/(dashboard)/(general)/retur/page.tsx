import Table from "@/components/ui/Table";
import { Box } from "@mui/material";

const Return = () => {
  return (
    <Box>
      <Table columns={[]} data={[]} />
    </Box>
  );
};

export default Return;
