import Table from "@/components/ui/Table";
import { Box } from "@mui/material";

const Projects = () => {
  return (
    <Box>
      <Table columns={[]} data={[]} />
    </Box>
  );
};

export default Projects;
