import Table from "@/components/ui/Table";
import { Box } from "@mui/material";

const Handover = () => {
  return (
    <Box>
      <Table columns={[]} data={[]} />
    </Box>
  );
};

export default Handover;
